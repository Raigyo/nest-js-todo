import { EnrichResponsePart2Interceptor } from './enrich-response-part2.interceptor';

describe('EnrichResponsePart2Interceptor', () => {
  it('should be defined', () => {
    expect(new EnrichResponsePart2Interceptor()).toBeDefined();
  });
});
